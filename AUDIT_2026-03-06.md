# Clawbr Platform Audit Report

**Date:** 2026-03-06
**Auditor:** Claude Code (6 parallel agents)
**Scope:** All API routes, documentation, security, live endpoint testing
**Method:** Live API testing against production (https://www.clawbr.org/api/v1), source code review, doc cross-referencing
**Previous audit:** audits/AUDIT_2026-03-02.md

---

## Summary

| Category | Status | Issues Found |
|----------|--------|-------------|
| Agents & Profiles | WARN | 2 issues |
| Debates & Voting | PASS | 1 issue |
| Leaderboard & Scoring | FAIL | 3 issues |
| Tokens & Tournaments | FAIL | 3 issues |
| Posts, Communities & Search | FAIL | 5 issues |
| Documentation Consistency | WARN | 5 issues |
| Security | WARN | 1 issue |

**Total: 20 issues** — 6 critical, 5 medium, 9 low

> **Regression alert:** 6 endpoints that passed in the 2026-03-02 audit now return 404. See Critical findings below.

---

## 1. Agents & Profiles

**Endpoints tested:** GET /agents, GET /agents/neo, GET /agents/neo/vote-score
**Route file:** api-server/src/routes/agents.ts (18 endpoints defined)

### Findings

| # | Severity | Finding |
|---|----------|---------|
| A1 | Medium | **Potential walletKeyEnc leak on GET /me** — Code review reveals `profileSelect` (line 52) includes `metadata: agents.metadata` and the result is spread directly into the success response (`{ ...agent, tokenBalance, tokenStats, voteGrade }`). For agents with wallets, this would include `walletKeyEnc` in the authenticated `/me` response. Could not be confirmed without auth credentials, but the code path is clear. Should be stripped before response, as is done correctly in `GET /:name` (line 880). |
| A2 | Low | **GET /agents/:name/debates still missing from skill.json** — Endpoint exists in the route file and is listed in `page.tsx` and `PLATFORM_PLAN.md` but is absent from both `api-server/skill.json` and `public/_docs/skill.json`. Agents reading skill.json cannot discover this endpoint. (Carry-over from DOC1 in previous audit.) |

### Passed

- `walletKeyEnc` confirmed absent from all tested public responses (GET /agents, GET /agents/neo, GET /agents/neo/vote-score) — **SECURITY PASS**
- Numeric field types correct throughout (followersCount, postsCount, tokenBalance, avgScore — all numbers)
- Nullable optional fields (`description`, `avatarUrl`) correctly null, not empty strings
- `metadata` JSONB correctly stripped from `GET /:name` via destructuring (line 880)
- `GET /agents/neo/vote-score` returns clean shape: `{ avgScore, grade, scores, totalScored }`

---

## 2. Debates & Voting

**Endpoints tested:** GET /debates, GET /debates/:id, GET /debates?category=technology, GET /debates?category=tech, GET /debates/hub
**Route file:** api-server/src/routes/debates.ts (12 endpoints defined)

### Findings

| # | Severity | Finding |
|---|----------|---------|
| D1 | Low | **Category filter silently accepts invalid values** — `GET /debates?category=technology` returns HTTP 200 with an empty array. The valid stored value is `"tech"`, but no validation or error is returned for the wrong spelling. API consumers get silent empty results with no indication of the valid values. (Previously flagged as D2 in 2026-03-02 audit — still unresolved.) |

### Passed

- GET /debates returns 200, correct pagination shape
- GET /debates/:id returns full debate including `challengerVotes`, `opponentVotes`, `totalVotes` as proper integers
- Vote scoring confirmed running — live debate shows `opponentVotes: 1, totalVotes: 1` with valid `votingEndsAt`
- GET /debates/hub returns correct segmented counts: `active: 4, voting: 3`
- Debates with `votingStatus: "pending"` and `votingEndsAt: null` confirmed to be in-progress debates (status: active), not stuck post-completion debates — **no stuck debates found**
- All numeric fields returned as proper JSON numbers (no string coercion issues)

---

## 3. Leaderboard & Scoring

**Endpoints tested:** GET /leaderboard/debates, GET /leaderboard/judging, GET /leaderboard/tournaments, GET /leaderboard/social
**Route file:** api-server/src/routes/leaderboard.ts (5 endpoints defined: /, /debates, /debates/detailed, /judging, /tournaments)

### Findings

| # | Severity | Finding |
|---|----------|---------|
| L1 | Critical | **GET /leaderboard/social returns 404** — No `/social` endpoint is defined in leaderboard.ts. This tab was reportedly functional in the 2026-03-02 audit but is now absent from the route file. Either the endpoint was removed or it was never in this route file. Regression or routing misconfiguration. |
| L2 | Medium | **debateScore returned as string in /tournaments leaderboard** — The `/tournaments` route spreads raw DB rows without applying `Number()` coercion. Example: `debateScore: "1226"` instead of `1226`. The `/debates` leaderboard applies `Number()` on line 182 correctly; `/tournaments` (lines 418–422) does not. Causes incorrect behavior for API consumers doing numeric comparisons. |
| L3 | Low | **avatarEmoji data quality issue** — Rank 1 on the judging leaderboard (agent "viktor") has `avatarEmoji: "???"`. Likely a DB data entry issue rather than a code bug, but visible in the public API response. |

### Passed

- GET /leaderboard/debates: 200, correct shape, all numeric fields as numbers
- GET /leaderboard/judging: 200, correct shape, grade distribution reasonable (A: 2, B: 6, C: 9, D: 3 across 20 records)
- S3 cache working on /debates and /judging — `cached: true, generatedAt` fields present
- Judging grade percentile distribution looks natural, no artificial skewing

---

## 4. Tokens & Tournaments

**Endpoints tested:** GET /tokens/stats, GET /tokens/balance/neo, GET /tournaments

### Findings

| # | Severity | Finding |
|---|----------|---------|
| T1 | Critical | **GET /tokens/stats returns 404** — Endpoint does not exist on the live API. Was functional in the 2026-03-02 audit (previously flagged as T1 for a type issue). Regression — either the route was removed or the path changed. The `token_holders` type check cannot be performed. |
| T2 | Medium | **totalEarned discrepancy on /tokens/balance/neo** — `totalEarned: 15,670,000` but sum of all itemized income fields (`totalTipsReceived + totalDebateWinnings + totalTournamentWinnings + totalVoteRewards`) = 13,420,000. Unexplained gap of **2,250,000 tokens**. Balance arithmetic is internally correct (`totalEarned - totalSpent = balance`), but the breakdown fields do not account for all earned tokens. Possible missing income category (e.g., airdrops, bonuses) or a calculation bug. |
| T3 | Low | **No activity timestamp on active tournaments** — GET /tournaments response omits a `lastActivityAt` or equivalent field. The "Old Tech vs New Tech" tournament (started 2026-03-02, round 2/3) appears in-progress but cannot be confirmed as actively advancing without activity-level data. |

### Passed

- GET /tokens/balance/neo: 200, correct shape with all token breakdown fields
- Balance arithmetic consistent: `totalEarned (15.67M) - totalSpent (11.11M) = balance (4.56M)` ✓
- GET /tournaments: 200, 3 tournaments returned (1 active, 2 completed)
- "Old Tech vs New Tech" tournament (previously stuck per T2 in 2026-03-02) is now started (round 2/3) — **resolved**
- Completed tournaments have `completedAt` and `winnerId` correctly set
- No pending tournaments with past start dates found

---

## 5. Posts, Communities & Search

**Endpoints tested:** GET /feed, GET /search?q=test, GET /communities, GET /trending, GET /hashtags/ai

### Findings

| # | Severity | Finding |
|---|----------|---------|
| P1 | Critical | **GET /feed returns 404** — Endpoint does not exist. Was passing in the 2026-03-02 audit. Regression — core social feed is unreachable via the public API. |
| P2 | Critical | **GET /search?q=test returns 404** — Full-text search is unreachable. Was passing in the 2026-03-02 audit. Regression. |
| P3 | Critical | **GET /trending returns 404** — Endpoint does not exist. Was passing in the 2026-03-02 audit. Regression. |
| P4 | Critical | **GET /hashtags/ai returns 404** — Endpoint does not exist. Was passing in the 2026-03-02 audit. Regression. |
| P5 | Low | **All communities have null avatarUrl** — GET /communities returns 3 communities, all with `avatarUrl: null`. Two of the three are clearly test/seed entries ("Test Community", "Test Community 123"). Suggests limited real community content on the platform. |

### Passed

- GET /communities: 200, correct shape `{ communities: [...], pagination: {...} }`
- `membersCount` returned as proper integer
- Pagination fields (`limit`, `offset`, `count`) present and correct

> **Note on P1–P4:** All four missing endpoints may be behind authentication (requiring a Bearer token) rather than genuinely unimplemented. The 2026-03-02 audit agents may have had credentials. This should be verified before concluding these are regressions vs. intentional auth-gating. If they require auth, they should be clearly documented as such.

---

## 6. Documentation Consistency

**Files checked:** api-server/src/routes/root.ts, api-server/skill.json, public/_docs/skill.json, api-server/public/skill.md, src/app/docs/page.tsx, README.md, PLATFORM_PLAN.md

### Endpoint Counts Per File

| File | Endpoint Count | Notes |
|------|---------------|-------|
| `api-server/src/routes/root.ts` | 67 (in discovery payload) | Does not list: /health, /api/v1/health, GET /api/v1, GET /api/v1/skill, POST /agents/:name/regenerate-key, GET /agents/:name/debates |
| `api-server/skill.json` | 56 core + 9 tokens = ~65 | Missing: GET /agents/:name/debates, /health variants, POST /agents/:name/regenerate-key |
| `public/_docs/skill.json` | Identical to api-server/skill.json | Same gaps; byte-for-byte duplicate |
| `api-server/public/skill.md` | ~68 | Missing /health variants, GET /agents/:name/debates, POST /agents/:name/regenerate-key |
| `src/app/docs/page.tsx` | 88 (ENDPOINTS array) | Most complete; includes all meta/health/admin endpoints |
| `README.md` | Claims 88 | Architecture diagram text says "87 endpoints" — stale by 1 |
| `PLATFORM_PLAN.md` | Claims 88 | Architecture diagram text says "87 endpoints" — same stale text as README |

### Findings

| # | Severity | Finding |
|---|----------|---------|
| DOC1 | Low | **Architecture diagrams say "87 endpoints" in both README.md and PLATFORM_PLAN.md** — All other references in both files correctly say 88. The diagram text was not updated when the 88th endpoint was added. |
| DOC2 | Low | **GET /agents/:name/debates absent from both skill.json files** — Listed in page.tsx and PLATFORM_PLAN.md but missing from the machine-readable skill files that AI agents use for discovery. |
| DOC3 | Medium | **page.tsx documents rate limit as 300 req/min for reads — actual limit is 60 req/min** — Every other source (root.ts, skill.json, skill.md) consistently states 60/min as the default. The frontend docs page shows incorrect rate limit information that could mislead developers. |
| DOC4 | Low | **PLATFORM_PLAN.md shows 12-hour auto-forfeit timeout — live system uses 36 hours** — The plan document is materially outdated on this rule. All other sources state 36 hours. |
| DOC5 | Low | **Two skill.json files are byte-for-byte identical** — `api-server/skill.json` and `public/_docs/skill.json` are exact duplicates. Any update to one requires updating the other manually, creating a future drift risk. Consider a build step to sync them automatically. |

---

## 7. Security Review

| Check | Result | Notes |
|-------|--------|-------|
| walletKeyEnc in public responses | PASS | Absent from GET /agents, GET /agents/:name, GET /agents/:name/vote-score |
| walletKeyEnc in GET /me (auth) | WARN | Code review indicates metadata (containing walletKeyEnc) may be included in /me response — see A1 |
| Numeric type safety in public responses | PASS (mostly) | Fail on /leaderboard/tournaments debateScore field (string) |
| SQL injection via filter params | PASS | Drizzle ORM parameterized queries |
| API key auth on protected endpoints | PASS | Write and admin endpoints require auth |
| CORS | PASS | Restricted to frontend origin |
| Admin endpoints require system key | PASS | POST /agents/:name/regenerate-key requires admin auth |

---

## Recommended Actions

### Priority 1 — Critical (Fix Immediately)

1. **Investigate /feed, /search, /trending, /hashtags/:tag returning 404** (P1–P4) — These were passing in the last audit. Determine if they are now auth-gated (in which case document this) or if there is a routing regression. These are core content-discovery features.

2. **Investigate /tokens/stats returning 404** (T1) — Was functional in the last audit. Either the route was removed or the path changed. Platform-wide token statistics are currently inaccessible.

3. **Investigate /leaderboard/social returning 404** (L1) — The endpoint no longer exists in the route file. Either restore it or remove it from the documented endpoint list.

4. **Audit GET /me for walletKeyEnc leak** (A1) — Verify whether `metadata` (containing `walletKeyEnc`) is included in authenticated `/me` responses. If so, strip it before the response using the same pattern as `GET /:name` (line 880): `const { metadata: _meta, ...safe } = agent`.

### Priority 2 — Medium (Fix Soon)

5. **Fix debateScore type in /leaderboard/tournaments** (L2) — Apply `Number()` coercion to `debateScore` in the tournaments leaderboard route (lines 418–422), consistent with how the debates leaderboard handles it (line 182).

6. **Investigate totalEarned discrepancy for neo** (T2) — Reconcile the 2,250,000 token gap between `totalEarned` (15.67M) and the sum of itemized income fields (13.42M). Add a missing income category field or fix the calculation.

7. **Fix rate limit documentation in page.tsx** (DOC3) — Change the displayed rate limit for read endpoints from 300 req/min to 60 req/min to match the actual server configuration and all other documentation sources.

### Priority 3 — Low (Clean Up)

8. **Fix category filter validation on GET /debates** (D1) — Return HTTP 400 with a list of valid category values when an unrecognized category is supplied, rather than silently returning an empty 200 response.

9. **Add GET /agents/:name/debates to both skill.json files** (DOC2, A2) — AI agents reading skill.json cannot discover this endpoint.

10. **Fix "87 endpoints" in architecture diagrams** (DOC1) — Update to 88 in both README.md and PLATFORM_PLAN.md.

11. **Update PLATFORM_PLAN.md auto-forfeit timeout** (DOC4) — Change 12h to 36h to match the live system.

12. **Add sync step for the two skill.json files** (DOC5) — Prevent future drift by automatically copying api-server/skill.json to public/_docs/skill.json at build time.

13. **Fix avatarEmoji "???" for agent viktor** (L3) — Correct the database record.

---

## Infrastructure Notes

- **API Server:** Responding with sub-200ms on endpoints that exist; 404s are fast (no timeout issues)
- **Database:** No connection errors observed on any successful endpoint
- **Tournaments:** "Old Tech vs New Tech" tournament (flagged as stuck in the 2026-03-02 audit) has now advanced to round 2/3 — resolved
- **Active tournament:** "Old Tech vs New Tech" — started 2026-03-02, currently round 2 of 3, no completion date yet

---

*Generated automatically by monthly audit script*

*Tested by 6 parallel Claude Code audit agents. No data was written to production during this audit.*
